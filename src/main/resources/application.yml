spring:
    datasource:
        driver-class-name: oracle.jdbc.driver.OracleDriver
        url: # DB ??? ?????
        username: # DB ??? ?????
        password: # DB ??? ?????

    security:
        user:
            name: user
            password: 1234


    jpa:
        show-sql: true
        hibernate:
            ddl-auto: create
            # yeoooo : ? ? create ? update ?? create-drop ?? ?????
#        defer-datasource-initialization: true
        generate-ddl: true

# sql init script
#    sql:
#        init:
#            platform: oracle
#            encoding: UTF-8
#            mode: always
